services:
  - type: web
    name: sengoku-chakai
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_BASE_URL
        sync: false
